fix(comps): stop fabricated comp addresses — use real provider comps only

The comps table was showing AI-invented placeholders ("123 Anywhere St",
"456 Somewhere Ave") whenever the data provider returned no comparables, and the
UI was preferring the AI's comps over the real ones.

Backend (/api/analyze): prompt now forbids inventing/guessing/placeholder
addresses — arv_comps must copy ONLY the real comps present in MARKET DATA, and
return an EMPTY array when there are none (the ARV is still estimated from the
Zestimate / $-per-sqft band). Applied to both the main qualification prompt and
the focused ARV recompute.

UI (AcquisitionsPanel): prefer the REAL provider comparables; only fall back to
the AI's list if the provider returned none, and in all cases filter out
fabricated placeholder addresses (regex guard). When no verified comps exist,
show an honest note instead of fake rows.

Verified: next build green, tsc --noEmit exit 0.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
