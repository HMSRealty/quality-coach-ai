feat(arv): full ARV report — untruncated reasoning + comparable-sales table

The MAO card now shows the complete ARV breakdown instead of a truncated
sentence:
  • Full "ARV basis" reasoning (the AI's math, no ellipsis).
  • Collapsible "Comparable Sales (N)" table: each comp's address, sold price,
    sqft, bd/ba and computed $/sqft, with a median $/sqft footer so the ARV math
    is fully auditable. normalizeComps tolerates the provider's varied field
    names (price/soldPrice/zestimate, sqft/livingArea, beds/bedrooms, …).
  • Generate Offer PDF now includes the ARV basis line + the comparables table
    for a complete report.

Lead page passes metadata.comparables through to the panel.

Verified: next build green, tsc --noEmit exit 0.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
