feat: owner-only data reset + dialer-hours pay calculator

Data reset (approved design): new POST /api/org/reset (edge, service role) —
owner-only (sub-roles 403), requires { confirm: "DELETE" }, scoped strictly to
the caller's own user_id/organization_id. Removes the account's recordings from
storage, then deletes call_uploads, training_snippets, agent_scorecards, leads
(cascades events), cold_callers and campaigns. Logins/org/team stay intact.
Settings gains a red "Danger Zone" card (owner-only) with a typed-DELETE confirm.

Dialer hours calculator (Payroll): new DialerHoursCalculator. parsePayableHours
understands the dialer export format "12 Hours 60 Mins." (= 13.00h) plus 12:30 /
12h 30m / bare numbers, multiplies by an hourly rate (default + per-row
override). Upload a CSV keyed on the "Payable Hours" column + a Name column
(matches CRM agents), or add rows manually; shows per-employee hours/pay and a
total. Template download included.

Verified: next build green, tsc --noEmit exit 0.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
