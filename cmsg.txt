feat: per-campaign webhook + leaderboard filters (no chart) + settings cleanup

Campaigns: new webhook_url column (migration 0012) — set an outbound webhook
when creating a campaign and edit it per-campaign in the expanded panel
(saves on blur).

Leaderboard: removed the behind-card sparkline chart; added an agent search box
and an On-pace/Behind filter on top of the existing 7/30/90-day range.

Settings: removed the Shift Type and Lead Export Webhook cards (shift type is
deprecated; webhooks now live under Webhooks & Integrations).

Verified: next build green, tsc --noEmit exit 0.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
